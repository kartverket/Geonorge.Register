@model Kartverket.Register.Models.RegisterItem
﻿@using Kartverket.Register.Helpers

@{
    ViewBag.Title = @Model.name;
    var documentOwner = ViewBag.documentOwner;
}

@section breadcrumb {

   @if (Model.register.containedItemClass == "Document")
   {
    <li><a href="/register/@Model.register.seoname">@Html.DisplayFor(model => model.register.name)</a></li>
    <li><a href="/register/versjoner/@Model.register.seoname/@documentOwner/@Model.seoname">Alle versjoner av @Model.name</a></li>
    <li class="active">@Html.DisplayFor(model => model.name)</li>
    
   }
   else
   {
    <li><a href="/register/@Model.register.seoname">@Html.DisplayFor(model => model.register.name)</a></li>
    <li class="active">@Html.DisplayFor(model => model.name)</li>
   }
}
<div class="page-header">
    <h2 class="h-md">@Html.DisplayFor(model => model.name)</h2>
    <hr>
</div>
<div class="col-lg-12 col-md-12 col-sm-12 col-xs-12">
    @*@if (Model.register.containedItemClass == "Document")
        {
            <div class="col-lg-12 col-md-12 col-sm-12 col-xs-12">
                <br />
        
                <ul class="nav nav-tabs nav-tabs-topp" style="clear:both">
                    <li class="tab active"><a aria-expanded="false" href="#gjeldende" id="gjeldendeLink" data-toggle="tab"><span class="glyphicon glyphicon-list-alt"></span> Gjeldende</a></li>
                    <li class="tab pull-right"><a aria-expanded="false" href="#forslag" id="forslagLink" data-toggle="tab"><span class="glyphicon glyphicon-home"></span>Forslag</a></li>
                    <li class="tab pull-right"><a aria-expanded="false" href="#historiske" id="historiskeLink" data-toggle="tab"><span class="glyphicon glyphicon-user"></span>Historiske</a></li>
                </ul>
            </div>
        }
        else
        {*@
            <div class="col-no-padding-xs col-xs-12 col-sm-10 col-md-10 col-lg-10">
                ID: <a href="/register/@Model.register.seoname">http://@Request.Url.Host@Request.Url.LocalPath </a>
                <p>
                    @Model.description
                </p>
                <span>Status: @Model.status.description <i class="statusIcon-@Model.statusId"></i></span><br>
                <span>Innsender: @Model.submitter.name</span><br>
                <span>Versjon: @Model.versionNumber</span><br>
            </div>
            <div class="col-no-padding-xs col-xs-12 col-sm-2 col-md-2 col-lg-2">
                <span class="small">Innsendt: @Html.DisplayFor(model => model.dateSubmitted)</span><br>
                <span class="small">Endret: @Html.DisplayFor(model => model.modified)</span>
            </div>
            <div class="col-no-padding-xs col-xs-12 col-sm-12 col-md-12 col-lg-12">
                <div class="page-header">
                    <hr>
                </div>
            </div>
        @*}*@

        <br />

        @if (Model.register.containedItemClass == "Organization")
        {
            @Html.Partial("_detailsOrganization")
        }
        @if (Model.register.containedItemClass == "Document")
        {
            @Html.Partial("_detailsDocuments")

            @*<div class="tab-content">
                <div class="tab-pane active" id="gjeldende">
                    @Html.Partial("_currentVersionDocument", Model)
                </div>
                <div class="tab-pane" id="historiske">
                    @Html.Partial("_histroicalDocuments", Model)
                </div>
                <div class="tab-pane" id="forslag">
                    @Html.Partial("_suggestedDocuments", Model)
                </div>
            </div>*@
        }
        @if (Model.register.containedItemClass == "Dataset")
        {
            @Html.Partial("_detailsDataset")
        }
        @if (Model.register.containedItemClass == "EPSG")
        {
            @Html.Partial("_detailsEpsg")
        }
        @if (Model.register.containedItemClass == "Register")
        {
            @Html.Partial("_detailsSubregister")
        }
        @if (Model.register.containedItemClass == "CodelistValue")
        {
            @Html.Partial("_detailsCodelistValue")
        }

    </div>

    <div class="col-lg-12 col-md-12 col-sm-12 col-xs-12">
        <p>            
            @if (Model.register.containedItemClass == "Document")
            {
                <a href="/register/versjoner/@Model.register.seoname/@documentOwner/@Model.seoname">Tilbake</a>
            }
            else { 
                <a href="/register/@Model.register.seoname">Tilbake</a>
            }
        

        @if (Request.IsAuthenticated)
        {
                
            if (Html.IsGeonorgeAdmin(System.Security.Claims.ClaimsPrincipal.Current.Claims) || 
                (Html.GetSecurityClaim(System.Security.Claims.ClaimsPrincipal.Current.Claims, "organization").ToLower() == Model.submitter.name.ToLower() && Html.IsGeonorgeEditor(System.Security.Claims.ClaimsPrincipal.Current.Claims)) ||
                (Html.GetSecurityClaim(System.Security.Claims.ClaimsPrincipal.Current.Claims, "organization") == @documentOwner && Html.IsGeonorgeEditor(System.Security.Claims.ClaimsPrincipal.Current.Claims)))
            {        
                if (Model.register.containedItemClass == "Organization")
                {
                    <span>| </span><a href="/organisasjoner/@Model.register.seoname/@Model.submitter.seoname/@Model.seoname/rediger">Endre</a>
                    <span>| </span><a href="/organisasjoner/@Model.register.seoname/@Model.submitter.seoname/@Model.seoname/slett">Slett</a>
                }
                if (Model.register.containedItemClass == "Document")
                {
                    @*<span> | </span><a href="/dokument/@Model.currentVersion.register.seoname/@documentOwner/@Model.currentVersion.seoname/rediger/">Endre</a>
                    <span> | </span><a href="/dokument/@Model.currentVersion.register.seoname/@documentOwner/@Model.currentVersion.seoname/slett">Slett</a>*@
                    <span> | </span><a href="/dokument/@Model.register.seoname/@documentOwner/@Model.seoname/ny">Foreslå ny versjon av @Model.name</a>
                }
                if (Model.register.containedItemClass == "Dataset")
                {
                    <span> | </span><a href="/dataset/@Model.register.seoname/@Model.submitter.seoname/@Model.seoname/rediger/">Endre</a>
                    <span> | </span><a href="/dataset/@Model.register.seoname/@Model.submitter.seoname/@Model.seoname/slett">Slett</a>
                }
                if (Model.register.containedItemClass == "EPSG")
                {
                    <span>| </span><a href="/epsg-koder/@Model.seoname/rediger">Endre</a>
                    <span>| </span><a href="/epsg-koder/@Model.seoname/slett">Slett</a>
                }
                if (Model.register.containedItemClass == "CodelistValue")
                {
                    <span>| </span><a href="/kodeliste/@Model.register.seoname/@Model.register.owner.seoname/@Model.seoname/rediger">Endre</a>
                    <span>| </span><a href="/kodeliste/@Model.register.seoname/@Model.register.owner.seoname/@Model.seoname/slett">Slett</a>
                }


            }
        }

    </p>
</div>
