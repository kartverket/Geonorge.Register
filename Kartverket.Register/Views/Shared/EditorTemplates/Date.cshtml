@model DateTime?
@{
    var values = ViewData.ModelMetadata.AdditionalValues;

    string cssClass = "";
    if (ViewData.ContainsKey("cssClass"))
    {
        cssClass = (string)ViewData["cssClass"];
    }
}
@Html.TextBox("", String.Format("{0:dd/MM/yyy H:mm}", Model.HasValue ? Model : null), new { @class = "form-control " + cssClass })